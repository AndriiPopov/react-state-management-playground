const Container = ({ children }) => (
  <div
    style={{
      padding: "20px",
      border: "1px solid black",
      borderRadius: "5px",
    }}
  >
    {children}
  </div>
);

export { Container };
